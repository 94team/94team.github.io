const debug = '?XDEBUG_SESSION_START=PHPSTORM'
const urls = {
  login: '/app/member/login' + debug,
  register: '/app/member/register' + debug,
  index: '/app/member/index' + debug
}
export default urls
